/* Strings */

const name = 'Jordan';
const middle = 'the hitman';
const last = 'Haines';

// Back ticks are the better way. No escaping needed and works well with HTML in JS

const hello = `My name is ${name}, Nice to meet you`;

console.log(hello);
